/**
 * Translation module — delegates to shared/translations.ts (via vn-translations shim).
 * Kept separate so future API-based translation can be added here.
 */
export { translateToVietnamese, importTranslations } from '../../shared/vn-translations'
